# 12. Modelo C4 V_1_0_3

## Nivel 1 — Diagrama de Contexto

```mermaid
flowchart TB
    admin["Administrador / TI<br/>Gestiona usuarios, roles y parámetros"]
    operador["Operador de Logística<br/>Carga pedidos y ejecuta VRPTW"]
    conductor["Conductor de Reparto<br/>Consulta rutas y registra entregas"]
    gerente["Gerente de Operaciones<br/>Consulta KPIs y reportes"]
    cliente["Cliente Final<br/>Consulta ETA de su pedido"]

    sistema(("EcoLogística Junín<br/>Huancayo, El Tambo y Chilca<br/>Plataforma de optimización de rutas<br/>VRPTW / Green VRP"))

    mapas["Servicio de Mapas / Tráfico<br/>OSM / Google Maps"]

    admin -->|"Administra usuarios y parámetros"| sistema
    operador -->|"Carga pedidos y ejecuta rutas"| sistema
    conductor -->|"Consulta ruta y registra entregas"| sistema
    gerente -->|"Consulta KPIs y reportes"| sistema
    cliente -->|"Consulta ETA"| sistema

    sistema -->|"Geocodificación, mapas y matrices de distancia"| mapas
```

---

## Nivel 2 — Diagrama de Contenedores

```mermaid
flowchart TB
    operador["Operador / Gerente<br/>Desktop Web Browser"]
    conductor["Conductor<br/>Mobile Browser<br/>PWA"]

    subgraph frontend["Capa de Presentación"]
        spa["Web SPA - Desktop<br/>React.js + Tailwind + Leaflet.js<br/><br/>Despacho, flota y dashboard"]
        pwa["Modo Conductor - PWA<br/>React.js + IndexedDB<br/><br/>Operación offline / online"]
    end

    subgraph backend["Capa de Servicios"]
        api["API Gateway / Backend<br/>Python 3.11 + FastAPI<br/><br/>REST, JWT y gestión transaccional"]
        engine["Motor de Optimización<br/>Python 3.11 + Google OR-Tools<br/><br/>VRPTW + Green VRP + Reoptimización"]
    end

    db[("Base de Datos<br/>PostgreSQL 15 + PostGIS 3<br/><br/>Pedidos, vehículos, rutas y geometrías")]

    mapas["APIs Externas de Mapas<br/>OSM / Google Maps<br/><br/>Cartografía, geocodificación y distancias"]

    operador -->|"HTTPS"| spa
    conductor -->|"HTTPS<br/>Offline / Online"| pwa

    spa -->|"REST / HTTPS - JSON"| api
    pwa -->|"REST / HTTPS<br/>Sincronización"| api

    api -->|"Invoca optimización"| engine
    api -->|"SQL / PostGIS"| db
    engine -->|"Consulta / persiste rutas"| db

    spa -->|"Carga mapas y tiles"| mapas
    api -->|"Geocodificación y matrices de distancia"| mapas
```

---

## Nivel 3 — Diagrama de Componentes del Backend

```mermaid
flowchart TB
    frontend["Frontend Apps<br/>React.js / PWA"]

    subgraph backend["API Backend - FastAPI"]
        auth["Security / Auth Component<br/>FastAPI + JWT<br/><br/>Autenticación, RBAC y validación"]

        fleet["Fleet Service<br/>Python<br/><br/>Vehículos y restricciones"]

        order["Order Service<br/>Python + GeoPy<br/><br/>Pedidos, CSV, geocodificación<br/>y ventanas de tiempo"]

        route["Route Service<br/>Python<br/><br/>Generación de rutas<br/>y re-optimizaciones"]

        kpi["Analytics & KPI Service<br/>Python<br/><br/>Consumo, CO₂, distancia<br/>y ahorro"]
    end

    engine["Google OR-Tools<br/>Python Wrapper<br/><br/>VRPTW + Green VRP"]

    db[("PostgreSQL + PostGIS<br/><br/>Persistencia de datos")]

    frontend -->|"Peticiones HTTPS / JWT"| auth

    auth -->|"Autoriza"| fleet
    auth -->|"Autoriza"| order
    auth -->|"Autoriza"| route
    auth -->|"Autoriza"| kpi

    fleet -->|"CRUD vehículos"| db
    order -->|"CRUD pedidos"| db

    route -->|"Datos de vehículos,<br/>pedidos y restricciones"| engine
    engine -->|"Secuencias, distancias y ETAs"| route

    route -->|"Persiste rutas"| db

    kpi -->|"Consulta rutas y datos"| db
    kpi -->|"Persiste KPIs"| db
```

---

## Descripción general

El modelo C4 de **EcoLogística Junín** representa la arquitectura de la plataforma en tres niveles:

1. **Nivel 1 — Contexto:** identifica los actores externos, el sistema principal y los servicios externos con los que interactúa.
2. **Nivel 2 — Contenedores:** muestra las principales aplicaciones, servicios, motor de optimización y base de datos que conforman la solución.
3. **Nivel 3 — Componentes:** detalla los principales componentes internos del backend desarrollado con FastAPI.

### Tecnologías principales

| Componente | Tecnología |
|---|---|
| Aplicación web | React.js + Tailwind CSS |
| Mapas en frontend | Leaflet.js |
| PWA del conductor | React.js + IndexedDB |
| Backend | Python 3.11 + FastAPI |
| Autenticación | JWT + RBAC |
| Optimización | Google OR-Tools |
| Base de datos | PostgreSQL 15 |
| Información espacial | PostGIS 3 |
| Cartografía | OpenStreetMap / proveedor externo |
| Protocolo de comunicación | REST / HTTPS / JSON |

### Alcance geográfico

La plataforma está orientada a la gestión y optimización de rutas de distribución en:

- Huancayo
- El Tambo
- Chilca

El sistema considera restricciones de capacidad, ventanas de tiempo, características de la flota y criterios de sostenibilidad mediante la aplicación de **VRPTW (Vehicle Routing Problem with Time Windows)** y **Green VRP (Green Vehicle Routing Problem)**.
